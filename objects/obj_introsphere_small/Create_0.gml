start_y = y

// Speed of the hover effect (higher numbers = faster bobbing)
hover_speed = random_range(0.1,0.5); 

// Distance of the hover effect (how many pixels up and down it travels)
hover_range = irandom(2); 